name: commandExample
enable_legacy_integration_attrs: true
apis:
  - name: commandJsonOut
    commands: 
      # run any command, you could cat .json file, or run some commands that produce a json output
      # the example just calls an API that returns json
      - run: cat ../../test/payloads/jsonPlaceholderTodo.json #json output is retrieved from this command
    custom_attributes:
      myCustomAttr: theValue